@page "{mode}"
@model Talent.WebAdmin.Pages.Setup.Tasks.ChecklistModel
@{
    ViewData["Title"] = "iTalent - Task (Chechklist)";
}

<checklist mode="@Model.Mode" :task-id="@Model.TaskId" :from-outside="@Model.FromOutside.ToString().ToLower()"></checklist>