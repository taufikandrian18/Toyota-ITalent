@page "{mode}"
@model Talent.WebAdmin.Pages.Setup.Tasks.HotspotModel
@{
    ViewData["Title"] = "iTalent - Task (Hotspot)";
}

<hotspot mode="@Model.Mode" :task-id="@Model.TaskId" :from-outside="@Model.FromOutside.ToString().ToLower()"></hotspot>
