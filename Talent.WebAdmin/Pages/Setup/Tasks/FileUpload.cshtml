@page "{mode}"
@model Talent.WebAdmin.Pages.Setup.Tasks.FileUploadModel
@{
    ViewData["Title"] = "iTalent - Task (File Upload)";
}

<file-upload mode="@Model.Mode" :taskid="@Model.TaskId" :from-outside="@Model.FromOutside.ToString().ToLower()"></file-upload>