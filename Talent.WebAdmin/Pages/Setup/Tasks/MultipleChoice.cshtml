@page "{mode}"
@model Talent.WebAdmin.Pages.Setup.Tasks.MultipleChoiceModel
@{
    ViewData["Title"] = "iTalent - Task (Multiple Choice)";
}

<multiple-choice mode="@Model.Mode" :task-id="@Model.TaskId" :from-outside="@Model.FromOutside.ToString().ToLower()"></multiple-choice>
