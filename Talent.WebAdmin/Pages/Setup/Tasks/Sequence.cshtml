@page "{mode}"
@model Talent.WebAdmin.Pages.Setup.Tasks.SequenceModel
@{
    ViewData["Title"] = "iTalent - Task (Sequence)";
}

<sequence mode="@Model.Mode" :task-id="@Model.TaskId" :from-outside="@Model.FromOutside.ToString().ToLower()"></sequence>