@page "{mode}"
@model Talent.WebAdmin.Pages.Setup.Tasks.EssayModel
@{
    ViewData["Title"] = "iTalent - Task (Essay)";
}

<essay-new mode="@Model.Mode" :task-id="@Model.TaskId" :from-outside="@Model.FromOutside.ToString().ToLower()"></essay-new>